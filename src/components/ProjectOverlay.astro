<div id="project-overlay" class="overlay hidden">
    <div class="overlay-content">
        <button id="close-overlay" class="close-btn">&times;</button>

        <div class="overlay-body">
            <!-- CAROUSEL -->
            <div class="carousel-container">
                <div id="carousel-track" class="carousel-track"></div>

                <button class="carousel-btn prev" id="prev-btn">&#10094;</button
                >
                <button class="carousel-btn next" id="next-btn">&#10095;</button
                >

                <!-- Dots -->
                <div class="marquee-strip">
                    <div class="marquee-content">
                        <span
                            >WEAKY &nbsp;&bull;&nbsp; PROJECT SHOWCASE
                            &nbsp;&bull;&nbsp; WEAKY &nbsp;&bull;&nbsp; PROJECT
                            SHOWCASE &nbsp;&bull;&nbsp;</span
                        >
                        <span
                            >WEAKY &nbsp;&bull;&nbsp; PROJECT SHOWCASE
                            &nbsp;&bull;&nbsp; WEAKY &nbsp;&bull;&nbsp; PROJECT
                            SHOWCASE &nbsp;&bull;&nbsp;</span
                        >
                        <span
                            >WEAKY &nbsp;&bull;&nbsp; PROJECT SHOWCASE
                            &nbsp;&bull;&nbsp; WEAKY &nbsp;&bull;&nbsp; PROJECT
                            SHOWCASE &nbsp;&bull;&nbsp;</span
                        >
                        <span
                            >WEAKY &nbsp;&bull;&nbsp; PROJECT SHOWCASE
                            &nbsp;&bull;&nbsp; WEAKY &nbsp;&bull;&nbsp; PROJECT
                            SHOWCASE &nbsp;&bull;&nbsp;</span
                        >
                    </div>
                </div>
            </div>

            <div id="carousel-dots" class="carousel-dots"></div>

            <div class="overlay-text">
                <h2 id="overlay-title">Project Title</h2>
                <div id="overlay-tech" class="project-tech"></div>
                <hr />
                <p id="overlay-desc">Detailed description...</p>

                <div class="overlay-extra">
                    <h3>Key Features</h3>
                    <ul id="overlay-features-list">
                        <!-- toue -->
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.7);
        backdrop-filter: blur(8px);
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 2000;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease;
    }

    .overlay.active {
        opacity: 1;
        pointer-events: auto;
    }

    .overlay-content {
        background: url(/img/site_bg.png);
        background-repeat: no-repeat;
        background-position: top center;
        background-size: auto 100%;
        background-attachment: local;
        width: 80%;
        height: 80%;
        border-radius: 20px;
        position: relative;
        overflow-y: auto;
        padding: 3rem;
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    }
    .overlay-content::-webkit-scrollbar {
        width: 8px;
    }
    .overlay-content::-webkit-scrollbar-thumb {
        background: #6366f1;
        border-radius: 10px;
    }

    .close-btn {
        position: absolute;
        top: 1.5rem;
        right: 1.5rem;
        background: none;
        border: none;
        color: white;
        font-size: 2.5rem;
        cursor: pointer;
        line-height: 1;
        z-index: 50;
    }

    .overlay-body {
        display: flex;
        flex-direction: column;
        gap: 2rem;
    }

    .carousel-container {
        margin: 0 auto;
        position: relative;
        width: 70%;
        aspect-ratio: 17.7 / 9;
        background: #111;
        border-radius: 12px;
        overflow: hidden;
        cursor: grab;
        user-select: none;
    }

.carousel-container img {
        border-radius: 12px;
}

    .carousel-container:active {
        cursor: grabbing;
    }

    .carousel-track {
        display: flex;
        width: 100%;
        height: 100%;
    }

    :global(.carousel-track img) {
        min-width: 100%;
        height: 100%;
        object-fit: cover;
        pointer-events: none;
        border-radius: 12px;
    }

    .carousel-btn {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        background: rgba(0, 0, 0, 0.5);
        color: white;
        border: none;
        padding: 1rem;
        cursor: pointer;
        font-size: 1.5rem;
        z-index: 10;
        transition: background 0.3s;
    }

    .carousel-btn:hover {
        background: #6366f1;
    }
    .prev {
        left: 0;
        border-radius: 0 5px 5px 0;
    }
    .next {
        right: 0;
        border-radius: 5px 0 0 5px;
    }

    /* Dots */
    .carousel-dots {
        display: flex;
        justify-content: center;
        gap: 0.8rem;
        margin-top: 1rem;
        margin-bottom: 1rem;
        width: 100%;
    }

    :global(.c-dot) {
        width: 10px;
        height: 10px;
        background: #444;
        border-radius: 50%;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    :global(.c-dot.active) {
        background: #6366f1;
        transform: scale(1.3);
    }

    .marquee-strip {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 30px;
        background: rgba(0, 0, 0, 0.85);
        backdrop-filter: blur(4px);
        z-index: 5;
        display: flex;
        align-items: center;
        overflow: hidden;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        pointer-events: none;
    }

    .marquee-content {
        display: flex;
        white-space: nowrap;
        animation: scroll-left 15s linear infinite;
    }

    .marquee-content span {
        color: #a5b4fc;
        font-size: 0.75rem;
        font-weight: bold;
        letter-spacing: 2px;
        text-transform: uppercase;
        padding-right: 2rem;
    }

    @keyframes scroll-left {
        0% {
            transform: translateX(0);
        }
        100% {
            transform: translateX(-50%);
        }
    }

    .carousel-btn {
        z-index: 10;
    }

    .overlay-text {
        padding: 0 1rem;
    }
    .overlay-text h2 {
        font-size: 2.5rem;
        color: #a5b4fc;
        margin-bottom: 1rem;
    }
    hr {
        border: 0;
        border-top: 1px solid #333;
        margin: 1.5rem 0;
    }
    .overlay-text p {
        color: #ccc;
        line-height: 1.8;
        font-size: 1.1rem;
    }

    .overlay-extra {
        margin-top: 2rem;
    }
    .overlay-extra h3 {
        color: #6366f1;
        margin-bottom: 1rem;
    }
    .overlay-extra ul {
        color: #888;
        padding-left: 1.2rem;
    }
    .overlay-extra li {
        margin-bottom: 0.5rem;
    }

    @media (max-width: 1000px) {
        .overlay-content {
            width: 95%;
            height: 95%;
            padding: 1.5rem;
        }
        .carousel-container {
            width: 100%;
        }
    }
    .hidden {
        display: none;
    }
</style>
